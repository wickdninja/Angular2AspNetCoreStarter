/* tslint:disable:no-unused-variable */

import { async, inject } from '@angular/core/testing';
import { MaskPipe } from './mask.pipe';

describe('Pipe: Mask', () => {
  it('create an instance', () => {
    let pipe = new MaskPipe();
    expect(pipe).toBeTruthy();
  });
});
